//recibe un parámetro y retorna si es mayor a cero, menor o igual a cero
let mayor_menor = numero => {
    // if(numero > 0){
    //         r = 'Mayor';
    // }else{
    // if(numero < 0){
    //         r = 'Menor';
    // }else{
    // r = 'Cero';
    // }
    // }
    // return r;
    
    //Misma Funcion con Operador Ternario
    // let variable = (r != undefined) ? r : 0;
    (numero > 0) ? r = 'Mayor': ((numero < 0) ? r = 'Menor' : r = 'Cero' );
    return r;
}

let a  = 2;
let b = 10;
function suma(num1, num2) {
    console.log(num1 + num2);
    return num1 + num2;
}
suma(1,3);
suma(a, b);
suma(a, 15);

function suma() {
    console.log(a + b);
    return a + b;
}
suma()
suma(1,2)

arr[numero]

for (let i = 0; i < array.length; i++) {
    const element = array[i];
    
}
